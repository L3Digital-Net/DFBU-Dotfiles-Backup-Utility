# DFBU — Task Completion

After completing a task:

```bash
ruff check .          # Lint passes
ruff format .         # Formatting consistent
pytest DFBU/tests/    # All tests pass
```

- Commit on `testing` branch only
- Never push to `main` without permission
